name: "JS/TS Scan"
disable-default-queries: false

# Explicit paths so CodeQL traverses submodules and app code
paths:
  - istampit-web
  - istampit-auth
  - istampit-action

paths-ignore:
  - '**/node_modules'
  - '**/dist'
  - '**/build'
  - '**/.next'
  - '**/out'
  - '**/coverage'
