// Removed redundant App Router NextAuth route for v4. Using pages/api/auth/[...nextauth].ts.
// This stub intentionally left blank to avoid type errors while preserving path if referenced.
export const runtime = 'edge'; // optional hint; no handlers exported.
