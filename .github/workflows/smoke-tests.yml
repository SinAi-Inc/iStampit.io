name: Retired API Smoke Tests (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke-test:
    runs-on: ubuntu-latest

    steps:
    - name: Retirement notice
      run: |
        echo "iStampit API was retired on 2026-05-14."
        echo "This workflow is preserved as an archival stub and no longer performs live smoke checks."
