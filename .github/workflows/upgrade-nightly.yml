name: Nightly Upgrade Receipts
on:
  schedule:
    - cron: '17 2 * * *'
  workflow_dispatch: {}
permissions:
  contents: read

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4.2.2
        with:
          submodules: true
      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v5
        with:
          python-version: '3.11'
      - name: Install opentimestamps dependencies
        run: pip install --require-hashes -r .github/workflows/requirements-ots.txt
      - name: Install CLI
        run: pip install --no-deps -e istampit-cli
      - name: Upgrade all receipts
        run: |
          istampit upgrade-all . --json > upgrade-report.json || true
      - name: Upload report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: upgrade-report
          path: upgrade-report.json
