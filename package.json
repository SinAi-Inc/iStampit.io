{
  "private": true,
  "workspaces": [
    "istampit-web",
    "istampit-auth",
    "istampit-action",
    "istampit-cli"
  ],
  "scripts": {
    "vercel-build": "git submodule update --init --recursive && npm install --workspaces && next build"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "next": "^15.4.6"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "next-auth": "^4.24.11"
  }
}
